const webApiEndpoint = '/api/query-stats'

module.exports = function(context) {
  //on a timer...call out to twitter api to find mentions

  //if any, extract query out of tweet

  //run compiler over query, get statQuery object

  //post statQuery object to api

  //get response from api, convert into tweet response

  //what is tweet response??? text or image?
}